# 运行模式
# SR_MOCK=1          全离线 mock 模式（零外部请求，回归用）
# SR_TOKENS=BTC,ETH  手动指定币种（跳过筛选，与 --tokens 互斥）
SR_MOCK=
SR_TOKENS=

# LLM（DeepSeek，06 票接入；init_chat_model 自动读取）
DEEPSEEK_API_KEY=
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
