# 运行模式（全部可选；留空 = 真实模式 + 自动筛选）
# SR_MOCK=1          全离线 mock 模式（零外部请求，回归用）
# SR_TOKENS=BTC,ETH  手动指定币种（跳过筛选，与 --tokens 互斥）
# SR_SCAN_DIR=       扫描器快照 CSV 目录（默认 ~/Projects/python_projects/BinanceApi/data/research）
# SR_SCAN_DATE=      扫描器快照固定日期 YYYY-MM-DD（默认取目录内最新 *_all.csv）
SR_MOCK=
SR_TOKENS=

# LLM（DeepSeek）：唯一必填项是 DEEPSEEK_API_KEY
DEEPSEEK_API_KEY=
# 可选覆盖（不填用默认值）：端点默认官方 https://api.deepseek.com；
# 模型默认 deepseek-chat，换模型（如 deepseek-reasoner）时再设：
# DEEPSEEK_MODEL=

# 爆仓数据源：OKX 公开端点（/api/v5/public/liquidation-orders），免费无需 key。
# 单所口径（OKX USDT 永续）；失败/无数据时爆仓字段全部 UNKNOWN（不中断批处理）。
